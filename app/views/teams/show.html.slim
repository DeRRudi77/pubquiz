= turbo_stream_from @team, (@captain ? :captain : :viewer)

.column
  .card
    header.card-header
      p.card-header-title = @team.game.name
    .card-content
      .content
        == render 'team', team: @team, captain: @captain
